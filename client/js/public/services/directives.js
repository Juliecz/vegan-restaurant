/**
 * Created by yuliya on 30.3.16.
 */
/*myApp
    .directive('dirContent', function () {
        return {
            restrict: 'E',
            //replace: true,
            //scope: {user: '='}, // This is one of the cool things :). Will be explained in post.
            scope: {
                url: ''
            },
            controller: contentCtrl,
            templateUrl: $scope.setContent() /*function () {
                //"/public/views/content.html"//+file
                var currentUrl = $location.absUrl();
                var templateFile = "";
                if(currentUrl.indexOf('public')>-1) {
                    templateFile = '/public/views/content.html';
                }
                else if (currentUrl.indexOf('admin')>-1) {
                    templateFile = '/admin/views/content.html';
                }
                $('#templateFile').attr('src', templateFile);
                return templateFile
            }*
        };
    });*/
